export type FountainItem = {
  fountainName: string;
  address: string;
  latitude: number;
  longitude: number;
  managedBy: string;
};

export type TrashBinItem = {
  cityName: string;
  address: string;
  locationDesc: string;
  latitude: number;
  longitude: number;
  binType: string;
};
